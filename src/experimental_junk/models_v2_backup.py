# Backup of model code before refactoring
# This was the working version before we split into modules
# Keep for reference in case we need to rollback

# DEPRECATED: Use src/models/loco.py instead

import torch
import torch.nn as nn

# Old monolithic implementation
# This file is kept for historical reference only

print("This is a backup file. Do not use directly.")
print("Refer to src/models/loco.py for the current implementation.")
